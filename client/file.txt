<div className="min-h-screen flex items-center justify-center bg-white px-4">
      <div className="w-full max-w-md bg-white rounded-3xl p-8 shadow-lg">
        <form onSubmit={handleSubmit} className="flex flex-col text-center">
          <h3 className="mb-3 text-4xl font-extrabold text-dark-grey-900">
            Sign In
          </h3>
          <p className="mb-6 text-grey-700">
            Enter your email and password
          </p>

          {/* Google Button */}
          <button
            type="button"
            className="flex items-center justify-center w-full py-4 mb-6 text-sm font-medium rounded-2xl bg-grey-300 hover:bg-grey-400 transition"
          >
            <img
              className="h-5 mr-2"
              src="https://raw.githubusercontent.com/Loopple/loopple-public-assets/main/motion-tailwind/img/logos/logo-google.png"
              alt="Google"
            />
            Sign in with Google
          </button>

          {/* Divider */}
          <div className="flex items-center mb-5">
            <hr className="flex-grow border-grey-500" />
            <span className="mx-4 text-grey-600 text-sm">or</span>
            <hr className="flex-grow border-grey-500" />
          </div>

          {/* Email */}
          <label className="mb-2 text-sm text-left text-grey-900">
            Email*
          </label>
          <input
            type="email"
            required
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            placeholder="mail@loopple.com"
            className="w-full px-5 py-4 mb-6 rounded-2xl bg-grey-200 text-sm outline-none focus:bg-grey-300"
          />

          {/* Password */}
          <label className="mb-2 text-sm text-left text-grey-900">
            Password*
          </label>
          <input
            type="password"
            required
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            placeholder="Enter a password"
            className="w-full px-5 py-4 mb-5 rounded-2xl bg-grey-200 text-sm outline-none focus:bg-grey-300"
          />

          {/* Remember + Forgot */}
          <div className="flex justify-between items-center mb-8 text-sm">
            <label className="flex items-center gap-2 cursor-pointer">
              <input
                type="checkbox"
                checked={remember}
                onChange={() => setRemember(!remember)}
                className="accent-purple-blue-500"
              />
              Keep me logged in
            </label>

            <button
              type="button"
              className="text-purple-blue-500 font-medium hover:underline"
            >
              Forget password?
            </button>
          </div>

          {/* Submit */}
          <button
            type="submit"
            className="w-full py-5 mb-5 rounded-2xl bg-purple-blue-500 hover:bg-purple-blue-600 text-white font-bold transition focus:ring-4 focus:ring-purple-blue-100"
          >
            Sign In
          </button>

          <p className="text-sm text-grey-900">
            Not registered yet?{" "}
            <span className="font-bold text-grey-700 cursor-pointer">
              Create an Account
            </span>
          </p>
        </form>
      </div>
    </div>